""" Fichier de fonctions d'Atari-go """

def point_is_empty(position):
	""" Vérifie que l'intersection passée en paramètre est vide
	Reçoit un tuple de coordonnées et renvoie un booléen

	"""
	pass

def examine_adjacent_points(character, position):
	""" Vérifie la présence d'un type d'élément donné sur les intersections adjacentes
	Reçoit un caractère, un tuple de coordonnées et renvoie une liste de tuples de coordonnées

	"""
	pass

def has_liberties(position):
	""" Vérifie la présence de libertés pour une position donnée
	Reçoit un tuple de coordonnées et renvoie un booléen

	"""
	pass

def has_groups(color, position):
	""" Vérifie la présence de groupes adjacents à une position donnée
	Reçoit une couleur, un tuple de coordonnées et renvoie un set de groupes

	"""
	pass
